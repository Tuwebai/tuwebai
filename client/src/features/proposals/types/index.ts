export interface ProposalSubmissionInput {
  nombre: string;
  email: string;
  pais?: string;
  tipo_proyecto: string;
  presupuesto?: string;
  plazo?: string;
  detalles: string;
  como_nos_encontraste?: string;
}
