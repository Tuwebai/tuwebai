import ClusterContentPage from '@/features/content/components/cluster-content-page';
import { turnosOnlineMedicosArgentinaConfig } from '@/features/content/health-cluster-pages';

export default function TurnosOnlineMedicosArgentinaPage() {
  return <ClusterContentPage config={turnosOnlineMedicosArgentinaConfig} />;
}
