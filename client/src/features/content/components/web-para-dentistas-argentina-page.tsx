import ClusterContentPage from '@/features/content/components/cluster-content-page';
import { webParaDentistasArgentinaConfig } from '@/features/content/health-cluster-pages';

export default function WebParaDentistasArgentinaPage() {
  return <ClusterContentPage config={webParaDentistasArgentinaConfig} />;
}
