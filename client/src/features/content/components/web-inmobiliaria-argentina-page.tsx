import ClusterContentPage from '@/features/content/components/cluster-content-page';
import { webInmobiliariaArgentinaConfig } from '@/features/content/real-estate-cluster-pages';

export default function WebInmobiliariaArgentinaPage() {
  return <ClusterContentPage config={webInmobiliariaArgentinaConfig} />;
}
