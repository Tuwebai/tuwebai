import ClusterContentPage from '@/features/content/components/cluster-content-page';
import { webParaKinesiologoArgentinaConfig } from '@/features/content/health-cluster-pages';

export default function WebParaKinesiologoArgentinaPage() {
  return <ClusterContentPage config={webParaKinesiologoArgentinaConfig} />;
}
