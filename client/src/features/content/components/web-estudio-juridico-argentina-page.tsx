import ClusterContentPage from '@/features/content/components/cluster-content-page';
import { webEstudioJuridicoArgentinaConfig } from '@/features/content/professionals-cluster-pages';

export default function WebEstudioJuridicoArgentinaPage() {
  return <ClusterContentPage config={webEstudioJuridicoArgentinaConfig} />;
}
