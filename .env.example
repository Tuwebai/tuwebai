# =============================================================================
# TuWebAI — Variables de entorno (servidor)
# Copiar a .env y rellenar valores. Nunca versionar .env con secretos reales.
# =============================================================================

# Entorno: development | production | test
NODE_ENV=development

# Puerto del servidor Express
PORT=5000

# Secreto para firmar sesiones (mín. 10 caracteres). Obligatorio.
SESSION_SECRET=your-session-secret-min-10-chars

# CORS: orígenes permitidos separados por comas
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://tuweb-ai.com,https://www.tuweb-ai.com

# SMTP (Nodemailer). En producción son obligatorios.
SMTP_USER=
SMTP_PASS=
# Opcionales (por defecto: Gmail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true

# Mercado Pago (opcionales en dev; recomendados en producción)
# MERCADOPAGO_ACCESS_TOKEN=
# MERCADOPAGO_WEBHOOK_SECRET=

# Firestore Admin SDK (recomendado para idempotencia distribuida de webhooks)
# FIREBASE_PROJECT_ID=
# FIREBASE_SERVICE_ACCOUNT_KEY=./firebase-service-account.json
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account", "...":"..."}
# Enforce auth en BFF por Firebase ID token (recomendado en produccion)
# ENFORCE_FIREBASE_AUTH=true

# URL base del frontend para retornos de Mercado Pago
FRONTEND_URL=https://tuweb-ai.com
# URL publica del backend (opcional, para notification_url de webhooks de Mercado Pago)
# BACKEND_URL=https://tuwebai-backend.onrender.com

# Log sink centralizado (opcional). Si se define, appLogger enviara JSON via HTTP POST.
# Desarrollo local recomendado (usa `npm run dev:log-sink` o `npm run dev:enterprise`)
# LOG_SINK_URL=http://127.0.0.1:5055/ingest
# LOG_SINK_API_KEY=tuwebai-local-log-key
# LOG_SINK_TIMEOUT_MS=1500
# Produccion (ejemplo)
# LOG_SINK_URL=https://logs.example.com/ingest
# LOG_SINK_API_KEY=your-log-sink-token
